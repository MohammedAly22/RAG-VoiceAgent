# =============================================================================
# Voice Agent · conda env "test-qwen"  (Python 3.12)
# Reused from the Sano stack. Runs, in THIS project:
#   • the ASR service            (QwenCleo — streaming Egyptian-Arabic ASR)
#   • the local LLM (vLLM)       (Qwen2.5-3B-Instruct, Section 4 deployment)
#   • the quantization benchmark (Section 3 — bf16 vs 4-bit NF4)
#
# Kept SEPARATE from the app env: vLLM/QwenCleo pin a torch build that would
# fight the app's lighter dependencies.
#
# Install order:
#   1. conda create -n test-qwen python=3.12 -y && conda activate test-qwen
#   2. pip install torch==2.9.1 --index-url https://download.pytorch.org/whl/cu128
#   3. pip install -r requirements/test-qwen.txt
#   4. pip install "websockets>=12"   # uvicorn 404s WS routes without it
#
# GPU: NVIDIA (CUDA 12.x driver, e.g. 535+). torch/vLLM wheels are cu128.
# =============================================================================

# --- core LLM serving + quantization ---
vllm==0.14.0
transformers==4.57.6
huggingface_hub==0.36.2
accelerate>=1.0
bitsandbytes>=0.43            # 4-bit NF4 quantization (Section 3)
openai==2.41.1                # OpenAI-compatible client (vLLM streaming)

# --- ASR (QwenCleo, Qwen3-based Egyptian ASR) ---
qwencleo-asr==0.2.1
onnxruntime==1.24.4

# --- service API / realtime ---
fastapi==0.136.3
uvicorn==0.49.0
websockets>=12
python-multipart==0.0.32
pydantic==2.13.4

# --- retrieval (hybrid dense + sparse) — shared with the app for CLI tests ---
sentence-transformers==5.6.0
rank-bm25==0.2.2

# --- audio / VAD ---
soundfile==0.14.0
soxr==1.0.0

# --- data / utils ---
numpy==2.2.6
PyYAML==6.0.3
requests==2.34.2
